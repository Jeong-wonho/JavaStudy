1. 버튼의 색상을 변경하는 곳을 reserv seat으로 받아오게 되었다. seat은 모든 것을 다 삭제 시킨다. 색상이 없으면 클릭시 그냥 자동으로 데이터가 입력된다.
2. 처음에 있는 데이터들을 확인하는건 문제가 되지 않는데!, 새로운 데이터를 입력하는 과정에서 시간이 존재 하지 않을 경우 데이터가 들어가지 않는다.
3. 데이터를 임의로 입력하였다. /다시 입실에서 시작 시간이 들어간다면, 퇴실시 데이터를 삭제 할 수 있을 것 같다./
4. 그리고 시간이 같을 경우 데이터를 삭제 하는 것이 퇴실 버튼을 누르면 작동 되게 진행했는데..! timecheck라는 함수를 만들지 않고, 그냥 reserve에 들어갓을때
시간이 같다면 모든 데이터를 삭제 하는 방향으로 진행하면 어떨까..?!

여러 테이블의 컬럼을 검색한다.
SELECT a.컬럼1, b.컬럼2~~~
--> JOIN사용

여러테이블의 행을 검색한다.
->집합연산자
SELECT 컬럼1 FROM a
UNION
SELECT 컬럼2
FROM b

처리순서를 갖는 한테이블의 컬럼들을 검색한다.
SELECT a.컬럼1, a.컬럼2, a.컬럼3
FROM a
WHERE a.컬럼1 = (SELECT b.컬럼 FROM b WHERE ~ )